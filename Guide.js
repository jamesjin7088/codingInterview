// 한 줄에 공백으로 데이터가 들어올 때
// const input = require("fs").readFileSync("example.txt").toString().split("");

// 여러 줄로 데이터가 들어올 때
// const input = requrie("fs").readFileSync("exapmle.txt").toString().split("\n");

// 어떤 데이터가 number형 이라면, 형 변환해주어야 한다.

// 백준에서 문제를 제출할 때는 /dev/stdin/으로 변경해주어야 한다.
// const input = requrie("fs").readFileSync("/dev/stdin").toString().split("\n");
